# Lecture 4：图算法地图

> 前置：[[02-Graph Traversal - DFS & BFS]]

图算法不是一堆需要死记的名字。先看题目要解决什么，再选择工具。

| 题目问法 | 图论类型 | 常用算法 | 当前学习阶段 |
| --- | --- | --- | --- |
| A 能否到 B？ | 可达性 / 连通性 | DFS、BFS、并查集 | 现在学 |
| 有几块互不相连区域？ | 连通分量 | DFS、BFS、并查集 | 现在学 |
| 网格中一整片区域有多大？ | 网格图遍历 | DFS、BFS | 现在学 |
| 最少走几步？每步代价相同 | 无权最短路径 | BFS | 接下来学 |
| 最短距离 / 最低费用？边代价不同 | 带权最短路径 | Dijkstra | 之后学 |
| 地图中快速找向目标的最短路 | 启发式最短路径 | A* | 在 Dijkstra 之后 |
| 是否有循环依赖？ | 有向图环检测 | DFS | 之后学 |
| 如何安排任务 / 课程顺序？ | 拓扑排序 | BFS（入度）/ DFS | 之后学 |
| 不断新增边后，是否属于同一组？ | 动态连通性 | 并查集（Union-Find） | 之后学 |
| 用最低总代价连通所有节点 | 最小生成树 | Kruskal、Prim | 之后学 |

## A* 与其他最短路算法的关系

不要把 A* 当作最早要学的算法。它依赖你先理解：路径、权重、优先队列和 Dijkstra。

```text
BFS：每条边代价相同，求最少步数
↓
Dijkstra：每条边代价可不同，求最低总代价
↓
A*：在 Dijkstra 基础上增加“离目标还有多远”的合理预估，常用于地图 / 游戏寻路
```

## 当前阶段应掌握什么

先掌握以下四件事即可：

1. 节点、边、有向 / 无向、权重；
2. 路径、连通、连通分量、环；
3. 邻接表与网格图；
4. DFS、BFS、visited 的意义。

## 推荐练习路径

1. **1791 Find Center of Star Graph**：认识边列表、节点与边。
2. **1971 Find if Path Exists in Graph**：连通性与遍历。
3. **733 Flood Fill**：网格图、访问相邻格子。
4. **200 Number of Islands**：连通分量计数。
5. **994 Rotting Oranges**：BFS 的分层扩散。

> 做每道题前，先写下：节点是什么？边是什么？图有方向吗？题目到底在问什么？

## 下一讲

[[04-Grid Graph & Connected Components]]：二维数组为什么能看成图，以及如何找到一整块相连的区域。
